{
  "name": "probashi-shield",
  "version": "1.0.0",
  "private": true,
  "description": "Probashi Shield - migrant worker anti-fraud platform (backend + frontend monorepo)",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "db:setup": "npm --prefix backend run db:push && npm --prefix backend run db:seed",
    "setup": "npm run install:all && npm run db:setup",
    "dev": "concurrently -n API,WEB -c green,cyan \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "import:bmet": "npm --prefix backend run import:bmet --"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
